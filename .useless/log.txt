旧版的数据在读取的时候就改掉,保存的时候也按新版保存.
目前先修理anchor部分的内容.
其他地方读取也要跟着改掉.

要改的地方
1 从卡片字段中读取JSON数据时,需要改进.
旧版(0.10.0以前)的groupinfo不存储其他groupinfo, 只存卡片ID, 现在要改进为也存groupinfo

2 链接卡片列表和链接卡片的层级信息不同步不一致

3 anchor对话框在取消链接时不同步

4 pairLi_pair_append 只更新了卡片的pair,没有更新selfdata对应的内容, 在这里应该是对应pair的卡片